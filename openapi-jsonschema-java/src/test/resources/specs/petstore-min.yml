openapi: 3.0.3
info:
  title: Petstore Minimal
  version: 1.0.0
paths: {}
components:
  schemas:
    Pet:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
    PetRef:
      $ref: '#/components/schemas/Pet'
