openapi: 3.0.3
info:
  title: Nullable and Enum Demo
  version: 1.0.0
paths: {}
components:
  schemas:
    User:
      type: object
      properties:
        nickname:
          type: string
          nullable: true
        role:
          type: string
          enum: [ADMIN, USER]

