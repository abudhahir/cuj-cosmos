openapi: 3.0.3
info:
  title: Composition Demo
  version: 1.0.0
paths: {}
components:
  schemas:
    PartA:
      type: object
      properties:
        a:
          type: string
    PartB:
      type: object
      properties:
        b:
          type: integer
    Composed:
      allOf:
        - $ref: '#/components/schemas/PartA'
        - $ref: '#/components/schemas/PartB'

